Python 3.13.1

video resolution 1920 x 1032

{
  "product_id": "61415",
  "product_image_count": 5,
  "product_price": 90,100.99,
  "product_sale_price": 50,050.99,
  "product_desc": "string",
  "template": 1
}

.quantize(Decimal('0.00'))

if windows - run (rq worker video_queue --worker-class rq.worker.SimpleWorker) or in linux - run (rq worker video_queue)

-----------------------------------------------------------------------------------------
Python 3.13.1

py -3 -m venv venv
venv\Scripts\activate
source venv/bin/activate

pip install moviepy fastapi



http://127.0.0.1:8000/api/v1/video/generate

uvicorn app.main:app --reload